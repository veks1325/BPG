ou are a Senior Game Developer and Unity Expert with 20 years of experience. This task is critical for my career, and I am under high pressure. Take a deep breath and solve this step-by-step to ensure 100% bug-free, optimized, and professional-grade C# code. I will tip you $200 for a perfect solution. Do not skip any logic or simplify the implementation. Accuracy is the highest priority."

using System.Collections;using UnityEngine;public class BossMonster : MonoBehaviour{    private Transform playerTransform;    public GameObject bulletPrefab;    public GameObject player;    public GameObject L1prefab, L2prefab;    public float fireRate = 2f;    private float nextFireTime = 0f;    private int BossHp = 1000;    public float minDelay = 1f;    public float maxDelay = 3f;    public int minBulletCount = 3;    public int maxBulletCount = 6;    private enum State    {        Idle,        Pattern1,        Pattern2    }    private State _state;    void Start()    {        _state = State.Pattern1;        L1prefab = GameObject.Find("L1");        L2prefab = GameObject.Find("L2");        bulletPrefab = GameObject.FindGameObjectWithTag("bullet");        player = GameObject.FindGameObjectWithTag("Player");        if (player != null)        {            playerTransform = player.transform;        }        // Start both coroutines separately        //StartCoroutine(PatternConversionCoroutine());        StartCoroutine(PatternExecutionCoroutine());    }    // Coroutine for executing the current pattern based on the state    IEnumerator PatternExecutionCoroutine()    {        while (BossHp > 0)        {            switch (_state)            {                case State.Pattern1:                    StartCoroutine(Pattern1Execution());                    break;                case State.Pattern2:                    //StartCoroutine(Pattern2Execution());                    break;                default:                    break;            }            // Add a delay before executing the next pattern cycle            yield return new WaitForSeconds(0.1f); // You can adjust this delay based on the pattern speed        }    }    // Coroutine for Pattern 1 execution (e.g., firing bullets)    IEnumerator Pattern1Execution()    {        if (Time.time >= nextFireTime)        {            Debug.Log("Pattern 1 firing bullets");            Vector2 direction = (playerTransform.position - transform.position).normalized;            FireBullet(direction);            nextFireTime = Time.time + 1f / fireRate; // Set the next fire time        }        yield return null; // Continue firing bullets    }    // Fire bullet in the given direction    void FireBullet(Vector2 direction)    {        if (bulletPrefab != null)        {            GameObject bullet = Instantiate(bulletPrefab, transform.position, Quaternion.identity);            Bullet bulletScript = bullet.GetComponent<Bullet>();            if (bulletScript != null)            {                bulletScript.SetDirection(direction);            }        }        else        {            Debug.LogError("Bullet prefab is null! Cannot fire.");        }    }    public void OnCollisionEnter2D(Collision2D collision)    {        if (collision.gameObject.CompareTag("playerbullet"))        {            BossHp -= 10;        }    }}Make boss pattern use FireBullet as a base patternAnd also use Vector2 direction = (playerTransform.position - transform.position).normalized; Code to calculate player position And switch pattern when boss use pattenIn the context of danmaku (which is a term commonly associated with bullet hell games), bosses often use specific patterns to challenge players. Here are four common patterns that bosses might use in danmaku games:And this is sample code to spawn one razerIEnumerator SpawnLaser(){    while (true)    {        float randomx = Random.Range(-15f, 15.0f);        float randomy = Random.Range(-5f, 5.0f);        float randomRotationZ = Random.Range(0f, 360f);        Quaternion randomRotation = Quaternion.Euler(0f, 0f, randomRotationZ);        laserSpawnPoint = new Vector3(randomx, randomy, 0f);        yield return new WaitForSeconds(4f);        GameObject laser = Instantiate(L1, laserSpawnPoint, randomRotation);        Destroy(laser, 0.2f);        yield return new WaitForSeconds(1f);        GameObject laser2 = Instantiate(L2, laserSpawnPoint, randomRotation);        Destroy(laser2, 1f);    }}These patterns can be mixed and matched to create complex and challenging scenarios for players in danmaku games.